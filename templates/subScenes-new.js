import React, { Component } from 'react';
import {
  View,
  Text,
  StyleSheet
} from 'react-native';
import {
} from '../components';

class {upperName}SubScene extends Component {
  render() {
    return (
      <View>
        <Text>{upperName}</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({
});

export {{upperName}SubScene};
