import React, { Component } from 'react';
import {
  View,
  Text,
  StyleSheet
} from 'react-native';
import {
} from './';

class {upperName}Component extends Component {
  render() {
    return (
      <View>
        <Text>{upperName}</Text>
      </View>
    );
  }
}

const styles = StyleSheet.create({
});

export {{upperName}Component};
